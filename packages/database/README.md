# Database

Placeholder estrutural do pacote de banco.

PostgreSQL, Prisma schema, migrations, seed e Prisma Client serao implementados na proxima etapa.

Nesta etapa nao ha Prisma, banco, migrations ou persistencia real.
