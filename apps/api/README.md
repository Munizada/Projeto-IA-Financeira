# API

Placeholder estrutural da API.

A API real sera implementada em etapa futura, depois do pacote `packages/database` com PostgreSQL/Prisma.

Nesta etapa nao ha NestJS, endpoints HTTP, webhooks, WhatsApp, IA ou integracoes externas.
